# Notion Integration Architecture

## Visual Flow Diagram

```
┌─────────────────────────────────────────────────────────────────┐
│                         USER INTERFACE                           │
│                    (video-editor-app/index.html)                 │
│                                                                   │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │  Header                                                   │  │
│  │  [Load JSON] [Load from Notion] [Save] [Confirm]        │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                   │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │  Notion Modal (when "Load from Notion" clicked)          │  │
│  │                                                           │  │
│  │  Filter: [All] [Pending] [Scheduled] [Confirmed]        │  │
│  │                                                           │  │
│  │  ┌────────────────────────────────────────────────────┐ │  │
│  │  │ 📊 Record 1                        [Pending]       │ │  │
│  │  │ Caption: "Video project..."                        │ │  │
│  │  │ ID: 251023100300 • Created: Oct 23                │ │  │
│  │  └────────────────────────────────────────────────────┘ │  │
│  │                                                           │  │
│  │  ┌────────────────────────────────────────────────────┐ │  │
│  │  │ 📊 Record 2                        [Scheduled]     │ │  │
│  │  │ Caption: "Another video..."                        │ │  │
│  │  │ ID: 251023100400 • Created: Oct 23                │ │  │
│  │  └────────────────────────────────────────────────────┘ │  │
│  │                                                           │  │
│  └──────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
                              ↓
                              ↓ User clicks record
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│                      JAVASCRIPT LAYER                            │
│                     (video-editor-app/app.js)                    │
│                                                                   │
│  Function: loadNotionRecords()                                   │
│  ├─ Fetches all records from worker                             │
│  ├─ Applies status filter if selected                           │
│  └─ Displays records in modal                                   │
│                                                                   │
│  Function: loadNotionRecord(formulaId)                          │
│  ├─ Fetches specific record's JSON by ID                        │
│  ├─ Parses JSON data                                            │
│  ├─ Calls loadData() to load into editor                        │
│  └─ Shows success notification                                  │
└─────────────────────────────────────────────────────────────────┘
                              ↓
                              ↓ HTTP Request
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│                     CLOUDFLARE WORKER                            │
│         https://notion-reader.debabratamaitra898.workers.dev    │
│                                                                   │
│  Endpoints:                                                      │
│  ┌────────────────────────────────────────────────────────────┐│
│  │ GET /                                                       ││
│  │ → Returns all records from Notion database                 ││
│  └────────────────────────────────────────────────────────────┘│
│                                                                   │
│  ┌────────────────────────────────────────────────────────────┐│
│  │ GET /?filter=pending                                        ││
│  │ → Returns filtered records by status                        ││
│  └────────────────────────────────────────────────────────────┘│
│                                                                   │
│  ┌────────────────────────────────────────────────────────────┐│
│  │ GET /?json_id=251023100300                                  ││
│  │ → Returns specific record with parsed JSON                  ││
│  └────────────────────────────────────────────────────────────┘│
│                                                                   │
│  Features:                                                       │
│  • CORS enabled for web access                                  │
│  • Automatic JSON parsing                                       │
│  • Error handling and validation                                │
│  • Formula ID lookup                                            │
└─────────────────────────────────────────────────────────────────┘
                              ↓
                              ↓ Notion API Request
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│                         NOTION API                               │
│                    (api.notion.com/v1)                          │
│                                                                   │
│  Database: 29451a6d097f8008aa06f33a562cfa0b                     │
│                                                                   │
│  Columns:                                                        │
│  ┌────────────────────────────────────────────────────────────┐│
│  │ ID (Formula)      │ Auto-generated unique ID               ││
│  │ Username (Title)  │ Primary identifier                     ││
│  │ Caption (Text)    │ Description of the video               ││
│  │ JSON (Text)       │ Video configuration JSON               ││
│  │ Status (Status)   │ Pending/Scheduled/Confirmed/Completed  ││
│  │ Endpoint (Select) │ API endpoint to use                    ││
│  │ Output URL (URL)  │ Link to rendered video                 ││
│  │ Editor (Formula)  │ Auto-generated editor URL              ││
│  └────────────────────────────────────────────────────────────┘│
│                                                                   │
│  Returns: Database records with all properties                  │
└─────────────────────────────────────────────────────────────────┘
                              ↓
                              ↓ Data flows back up
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│                      VIDEO EDITOR PREVIEW                        │
│                                                                   │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │                                                           │  │
│  │                   Canvas Preview                          │  │
│  │                   (1080x1920)                            │  │
│  │                                                           │  │
│  │              [Video/Image Rendering]                      │  │
│  │                                                           │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                   │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │  Timeline                                                 │  │
│  │  ▬▬▬▬▬▬▬▬▬▬▬▬▬▬▬▬▬▬▬▬▬▬▬▬▬▬▬▬▬▬▬▬▬▬▬▬▬▬▬▬▬▬▬▬▬▬▬▬▬▬▬  │  │
│  │  Video: [Clip 1] [Clip 2] [Clip 3]                      │  │
│  │  Text:  [Caption 1]    [Caption 2]                       │  │
│  │  Audio: [Background Music]                               │  │
│  └──────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
```

## Data Structure Flow

### 1. Notion Record Structure
```json
{
  "id": "page-id-uuid",
  "properties": {
    "ID": { "formula": { "string": "251023100300" } },
    "Username": { "title": [{ "plain_text": "user123" }] },
    "Caption": { "rich_text": [{ "plain_text": "My video" }] },
    "JSON": { "rich_text": [{ "plain_text": "{...}" }] },
    "Status": { "status": { "name": "Pending" } },
    "Endpoint": { "select": { "name": "vid-1.5" } }
  }
}
```

### 2. Worker Transforms to
```json
{
  "formula_id": "251023100300",
  "username": "user123",
  "caption": "My video",
  "status": "Pending",
  "endpoint": "vid-1.5",
  "json_parsed": {
    "audioUrl": "audio.mp3",
    "duration": 30,
    "clips": [...]
  }
}
```

### 3. Editor Loads as
```javascript
{
  audioUrl: "audio.mp3",
  duration: 30,
  clips: [
    { videourl: "...", start: 0, duration: 5 },
    { imageurl: "...", start: 5, duration: 3 }
  ],
  captions: [...],
  overlay: {...}
}
```

## Component Interaction

```
┌──────────────┐
│   User       │
└──────┬───────┘
       │ clicks "Load from Notion"
       ↓
┌──────────────────────┐
│  showNotionModal()   │
│  loadNotionRecords() │
└──────┬───────────────┘
       │ fetch(worker_url)
       ↓
┌──────────────────────┐
│  Cloudflare Worker   │
│  GET /               │
└──────┬───────────────┘
       │ query Notion API
       ↓
┌──────────────────────┐
│  Notion Database     │
│  Returns records     │
└──────┬───────────────┘
       │ JSON response
       ↓
┌──────────────────────┐
│  Display in Modal    │
│  (record cards)      │
└──────┬───────────────┘
       │ user clicks record
       ↓
┌──────────────────────┐
│ loadNotionRecord(id) │
└──────┬───────────────┘
       │ fetch(worker_url/?json_id=ID)
       ↓
┌──────────────────────┐
│  Cloudflare Worker   │
│  GET /?json_id=ID    │
└──────┬───────────────┘
       │ query Notion API
       ↓
┌──────────────────────┐
│  Notion Database     │
│  Returns JSON field  │
└──────┬───────────────┘
       │ parsed JSON
       ↓
┌──────────────────────┐
│  loadData(json)      │
│  Existing function   │
└──────┬───────────────┘
       │ render
       ↓
┌──────────────────────┐
│  Canvas Preview      │
│  Timeline Display    │
└──────────────────────┘
```

## File Dependencies

```
video-editor-app/
├── index.html
│   ├── Defines UI structure
│   ├── Includes Notion modal
│   └── Links to app.js and styles.css
│
├── app.js
│   ├── Implements Notion functions
│   ├── Handles API communication
│   └── Manages data loading
│
└── styles.css
    ├── Styles Notion button
    ├── Styles modal and filters
    └── Styles record cards

workers/
└── notion-reader.js
    ├── Cloudflare Worker code
    ├── Handles Notion API calls
    └── Provides REST endpoints
```

## Security & Authentication

```
┌─────────────────────┐
│  Video Editor App   │
│  (Public)           │
└──────┬──────────────┘
       │ No auth required
       │ CORS: *
       ↓
┌─────────────────────┐
│  Cloudflare Worker  │
│  (Public endpoint)  │
└──────┬──────────────┘
       │ Uses stored secret
       │ Authorization: Bearer TOKEN
       ↓
┌─────────────────────┐
│  Notion API         │
│  (Authenticated)    │
└─────────────────────┘
```

## Error Handling Flow

```
User Action
    ↓
Try to fetch from Worker
    ↓
    ├─ Success → Display records
    │
    └─ Error → Show error message
              ├─ Network error
              ├─ Worker not accessible
              ├─ Invalid response
              └─ No records found
```

---

This diagram shows the complete architecture of the Notion integration with the video editor app.
